@if(Request.IsAuthenticated) {
    <text>Welcome <b>@Context.User.Identity.Name</b>!
    
    [ @Html.ActionLink("Log Off", MVC.Account.Logout()) ]</text>
}
else {
    @:[ @Html.ActionLink("Login", MVC.Account.Login()) ]
    @:[ @Html.ActionLink("Register", MVC.Account.Signup()) ]
}
